services:
  bilibili-bot-dev:
    image: node:22-alpine
    working_dir: /app
    volumes:
      - .:/app
    env_file:
      - .env
    command: sh -c "npm install && npm run dev"
    restart: unless-stopped
