version: "3.9"
services:
  bot:
    image: node:20-alpine
    working_dir: /app
    volumes:
      - ../../:/app
    env_file:
      - ../../.env
    command: sh -c "npm install && npm run dev"
    restart: unless-stopped
